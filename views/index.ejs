<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
</head>
<body class="fade-in">
  <div class="navbar main-nav">
    <a href="/">Home</a>
    <% if (user) { %>
      <a href="/profile">Profile</a>
      <a href="/books/add">Add Book</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>

  <div class="container">
    <h1 class="main-title">Книжкова галерея</h1>

    <form method="GET" action="/" class="search-form standalone-search">
      <div class="search-group">
        <label for="search" class="search-label">Пошук книги або автора:</label>
        <input
          type="text"
          id="search"
          name="q"
          value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
          placeholder="Введіть назву або автора..."
          class="search-input"
        />
      </div>

      <% if (sortOrder) { %>
        <input type="hidden" name="sort" value="<%= sortOrder %>">
      <% } %>

      <% if (selectedGenres && selectedGenres.length > 0) { %>
        <% selectedGenres.forEach(g => { %>
          <input type="hidden" name="genre" value="<%= g %>">
        <% }) %>
      <% } %>

      <button type="submit" class="btn-primary">Пошук</button>
    </form>

    <button id="filter-toggle" class="btn-primary filter-toggle" type="button">Фільтр</button>

    <div class="search-container" id="search-container">
      <form method="GET" action="/" class="search-form" id="filter-form">
        <input
          type="hidden"
          name="q"
          value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
        >

        <div class="filter-group">
          <label class="filter-label">Жанри:</label>

          <div class="genre-tags">
            <label class="genre-tag">
              <input
                type="checkbox"
                id="all-genres"
                <%= !selectedGenres || selectedGenres.length === 0 ? 'checked' : '' %>
              >
              <span>Усі</span>
            </label>

            <% genres.forEach(g => { %>
              <label class="genre-tag">
                <input
                  type="checkbox"
                  name="genre"
                  value="<%= g %>"
                  <%= selectedGenres && selectedGenres.includes(g) ? 'checked' : '' %>
                >
                <span><%= g %></span>
              </label>
            <% }) %>
          </div>
        </div>

        <div class="filter-group">
          <label for="sort" class="filter-label">Сортувати:</label>
          <select id="sort" name="sort" class="filter-select">
            <option value="asc" <%= sortOrder === 'asc' ? 'selected' : '' %>>A→Z</option>
            <option value="desc" <%= sortOrder === 'desc' ? 'selected' : '' %>>Z→A</option>
          </select>
        </div>
      </form>
    </div>

    <div class="book-grid">
      <% if (books && books.length > 0) { %>
        <% books.forEach(book => { %>
          <div class="book-card">
            <a href="/books/<%= book._id %>">
              <% if (book.coverImage) { %>
                <img src="<%= book.coverImage %>" alt="Обкладинка" class="book-cover" />
              <% } else { %>
                <div class="book-placeholder">Без фото</div>
              <% } %>
              <h3><%= book.title %></h3>
            </a>
          </div>
        <% }) %>
      <% } else { %>
        <p>Немає книг для відображення.</p>
      <% } %>
    </div>
  </div>

  <script>
    const filterToggle = document.getElementById('filter-toggle');
    const searchContainer = document.getElementById('search-container');
    const filterForm = document.getElementById('filter-form');
    const genreInputs = document.querySelectorAll('input[name="genre"]');
    const sortSelect = document.getElementById('sort');
    const allGenresCheckbox = document.getElementById('all-genres');

    function openFilters() {
      searchContainer.style.display = 'block';
      searchContainer.style.opacity = '1';
      searchContainer.style.transform = 'translateY(0)';
    }

    function closeFilters() {
      searchContainer.style.opacity = '0';
      searchContainer.style.transform = 'translateY(-16px)';
      setTimeout(() => {
        searchContainer.style.display = 'none';
      }, 300);
    }

    filterToggle.addEventListener('click', function () {
      const isClosed =
        searchContainer.style.display === 'none' ||
        searchContainer.style.display === '';

      if (isClosed) {
        openFilters();
      } else {
        closeFilters();
      }
    });

    const hasCheckedGenre = Array.from(genreInputs).some(input => input.checked);
    const hasSortChanged = sortSelect && sortSelect.value !== 'asc';

    if (hasCheckedGenre || hasSortChanged) {
      openFilters();
    }

    genreInputs.forEach(input => {
      input.addEventListener('change', function () {
        if (this.checked) {
          allGenresCheckbox.checked = false;
        }

        const anyChecked = Array.from(genreInputs).some(item => item.checked);
        if (!anyChecked) {
          allGenresCheckbox.checked = true;
        }

        filterForm.submit();
      });
    });

    if (sortSelect) {
      sortSelect.addEventListener('change', function () {
        filterForm.submit();
      });
    }

    if (allGenresCheckbox) {
      allGenresCheckbox.addEventListener('change', function () {
        if (this.checked) {
          const params = new URLSearchParams(window.location.search);
          const currentSearch = params.get('q');
          const currentSort = params.get('sort');

          let targetUrl = '/';
          const nextParams = new URLSearchParams();

          if (currentSearch) {
            nextParams.set('q', currentSearch);
          }

          if (currentSort) {
            nextParams.set('sort', currentSort);
          }

          const queryString = nextParams.toString();
          if (queryString) {
            targetUrl += '?' + queryString;
          }

          window.location.href = targetUrl;
        }
      });
    }
  </script>
</body>
</html>