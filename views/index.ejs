<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
</head>
<body class="fade-in">
  <div class="navbar">
    <a href="/">Home</a>
    <% if (user) { %>
      <a href="/profile">Profile</a>
      <a href="/books/add">Add Book</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>

  <div class="container">
    <h1>📚 Книжкова галерея</h1>

    <!-- Пошук -->
    <form method="GET" action="/" style="margin-bottom: 20px;">
      <div class="form-group">
        <label for="search">Пошук книги або автора:</label>
        <input type="text" id="search" name="q" value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>" placeholder="Введіть назву або автора...">
      </div>
    </form>

  
    <div class="book-grid">
  <% if (books && books.length > 0) { %>
    <% books.forEach(book => { %>
      <div class="book-card">
        <a href="/books/<%= book._id %>">
          <% if (book.coverImage) { %>
            <img src="<%= book.coverImage %>" alt="Обкладинка" class="book-cover">
          <% } else { %>
            <div class="book-placeholder">Без фото</div>
          <% } %>
          <h3><%= book.title %></h3>
        </a>
      </div>
    <% }) %>
  <% } else { %>
    <p>Немає книг для відображення.</p>
  <% } %>
</div>

  </div>
</body>
</html>
