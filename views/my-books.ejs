<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Мої книги</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="navbar">
    <a href="/">Home</a>
    <a href="/profile">Profile</a>
    <a href="/books/add">Add a book</a>
    <a href="/logout">Logout</a>
  </div>

  <div class="container">
    <h2>Мої книги</h2>

    <div class="book-grid">
      <% if (books && books.length > 0) { %>
        <% books.forEach(book => { %>
          <div class="book-card">
            <% if (book.coverImage) { %>
              <img src="<%= book.coverImage %>" class="book-cover" alt="Обкладинка">
            <% } %>
            <h3><%= book.title %></h3>
            <p><em><%= book.author %></em></p>
            <form action="/books/<%= book._id %>/delete" method="POST" style="margin-top: 10px;">
              <button type="submit" class="delete-button">🗑️ Видалити</button>
            </form>
          </div>
        <% }) %>
      <% } else { %>
        <p>У вас ще немає доданих книг.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
